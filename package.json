{
  "name": "tgperekup",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @tgperekup/shared && npm run build -w @tgperekup/server && npm run build -w @tgperekup/client",
    "typecheck": "npm run build -w @tgperekup/shared && npm run typecheck --workspaces --if-present",
    "lint:fix": "eslint . --fix",
    "test": "npm run test --workspaces --if-present",
    "dev": "docker compose up --build"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.0.0",
    "@typescript-eslint/parser": "^8.0.0",
    "eslint": "^9.0.0",
    "typescript": "^5.7.0"
  }
}
