/** Cost of diagnostics on a garage or market car (in dollars). */
export const DIAGNOSTICS_COST = 200;

/** Cost to refresh the market on a buy cell (in dollars). */
export const MARKET_REFRESH_COST = 500;

/** Cost to purchase one energy unit (in dollars). */
export const ENERGY_PURCHASE_COST = 500;

/** Maximum energy a player can hold. */
export const MAX_ENERGY = 3;

/** Repair cost discount multiplier on the Special Repair cell (5% off). */
export const SPECIAL_REPAIR_DISCOUNT = '0.95';
